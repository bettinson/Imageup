# Image uploader

Get started with:

`bundle install --without production && rake db:migrate`

Run tests with:

`rake`

Eventual roadmap:

- Index page with thumbnails created by worker tasks
- Users
- Tagging
- Searching
