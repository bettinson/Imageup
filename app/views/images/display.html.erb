<p><%= @image.title %></p>
<p><%= @image.path %></p>
<% if Rails.env.production?%>
  <% image_path = "/i/" + @image.path %>
<% else %>
  <% image_path = "#{Rails.root}/public/images/" + @image.path %>
<% end %>

<%= link_to image_tag(image_path), images_display_url(id: @image.id) %>
